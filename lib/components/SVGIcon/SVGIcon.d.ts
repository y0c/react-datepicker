import * as React from 'react';
import { IDatePicker } from '../../common/@types';
interface Props extends IDatePicker.SVGIconProps {
    id: string;
}
declare const SVGIcon: React.FunctionComponent<Props>;
export default SVGIcon;
